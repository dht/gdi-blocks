import { id } from '../Services';

export const params: IWidgetParams = {
    id,
    schema: {
        strings: {  },
        colors: { },
        extra: {
            servicesDataset: { fieldType: 'dataset', order: 3 },
        },
    },
};
